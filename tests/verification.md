# 验证范围

## v0.5.0 首次入口与切换 · 2026-09-13

新增首次阅读／生产选择、两路线新手教程、任意阶段切换和跨工具 handoff 模板。当前明确用途免重复问询；阅读不进入媒体配置，手动／定时与用途分别处理。主 Agent 复看入口、配置与教程之间的关系，补充“本次阅读覆盖旧漫画偏好”，并核对已有媒体任务和订阅不会随临时切换被误删或重复创建。

按用户指定优先委派 GLM-5.3-Flash：两路 Claude CLI 分别承担文档审校和行为规则推演，传入待发布文件正文，不开放执行工具。一条完成，返回的实际模型标识为 GLM-5.3-Flash：八种首次调用、明确文字请求、缺视频配置、切回阅读、取消问询、跨工具状态不明及维护场景未发现规则冲突。主 Agent 复核结果与建议；这是规则文本推演，不是新宿主的安装或真实媒体端到端测试。另一条文档审校 240 秒超时，无有效结果，不计为通过；由主 Agent 完成文档复核。

本次 Skill 格式校验、65 个清单文件的存在性、相对文件链接与 Markdown 锚点检查通过；重新运行 9 项候选导入器测试全部通过。打包脚本已实际构建并校验压缩包。媒体代码未变，复用下方既有测试和案例证据；四份案例视频 SHA-256 与原结果一致，没有新增付费生成。

明确清单检查未发现本地实际视频密钥值或个人绝对路径；不包含 .env、runs/、私有 state.json、会话记录或浏览器登录态。最终发布包与远端提交、附件摘要在发布步骤核验；远端发布事实以对应 GitHub Release 为准，不把本文件当作上传回执。本轮不创建真实定时任务、不迁移私人材料、不操作视频平台，其他 Agent 工具的真实接续尚未逐一实跑。

## v0.4.0 本地更新 · 2026-09-13

同日按确认的年度命名约定迁移了当前未发布版本，文档、版本文件、发布说明文件名和打包引用同步更新。导入器的 User-Agent 和默认打包目录改为读取 VERSION。仅重新检查相关导入测试、Skill 格式和安装包，未改动的媒体功能复用下方已有 53 项通过记录；不将改号当作重新付费试跑。旧发布版本和原始运行记录保留原编号与时间。

本轮 53 项测试全部通过，其中 9 项为新候选导入器测试，其余为媒体、配音、服务契约及续跑回归。新增测试覆盖 HTML 解析与实体、出处定位、固定版本、按日可复现抽样、跨期精确去重、输入边界、缺量、部分读取失败及截断目录。服务生成响应仍为模拟，没有提交新的付费媒体任务。

真实联网导入：固定 A-Joke 提交的 131 份日刊中读取 4 份，取得 30 条未审候选，无失败日刊或数量缺口。候选与 CLI 原始输出存在被忽略的 runs/，不进入分享包。

Skill 格式验证通过。新增长稿经中文稿件检查无硬性问题，约 1100 汉字；虚构对白与口播句长已按文体复看，未录音计时、未做观众测试。文档和验收规格同步为 10+1，相声计入十个、长稿独立。

两路 Claude CLI 返回研究，实际模型路由为 GLM-5.3-Flash；Antigravity 直接网页读取未成功，对已有素材的筛选完成。主 Agent 复核重点来源并补政治讽刺资料，见[研究表](../docs/research-v0.4.0.md)。不代表全部模型候选均已核验，本轮未重新执行完整日报到视频生产。

本地 0.4.0 尚未发布 GitHub。旧 Archify 图未改动，保留 v1.3.0 媒体路线的验证结论，新增文字分支在 README 和生产文档说明。

## 以下为 v1.3.0 历史验证

记录日期：2026-09-12。不同阶段分别验收，不把模拟测试或单镜试片称为整条视频和发布均已跑通。

| 阶段 | 已有证据 | 边界 |
| --- | --- | --- |
| 收集与改编 | 实际读取过网络故事，公开来源与文案见 examples 中的 story.json | 不代表每次运行已重新搜索 |
| GPT 分镜 | 两个故事分别生成并查看过独立竖图；公开教学样例保存在 examples/afanti | 新故事仍需实际生图 |
| 本地配音视频 | 三镜十五秒样片已生成、解码和查看；含系统配音、字幕与轻微镜头推进 | 是配音图文视频，不是 H3 动作动画 |
| 真实 H3 单镜 | 768P、请求四秒的首帧图生视频成功并下载；实际 4.458333 秒，768×1344，24 fps，H.264/AAC，音视频均通过解码 | 仅一镜试片；画面动作已抽帧查看，未完整逐字听验模型台词 |
| 服务使用量 | 成功任务报告 output_seconds=4、input_image_count=1；按当日费率输出估算 2 元 | 未查询真实账单，不称已确认扣费 |
| 真实 H3 三镜成片 | 代码链路已实现，服务商状态用模拟响应测试，媒体合成为真实 FFmpeg | 尚未用三镜真实 H3 任务完成整片验收 |
| 当日最佳编排 | 已加入来源筛选、分项比较、最佳选择及图文稿交付说明和模板 | 是新整理的 Agent 工作流，本轮未重新执行整日内容生产 |
| 平台上传 | 不在本 Skill 的生产范围 | 本地待用户上传，无平台草稿或发布回执 |

真实服务曾有一次明确失败，本地保留错误与任务状态，没有自动重复提交。后续使用通用人物形象描述的单镜请求成功；这不证明失败原因一定是人名，也不构成删除名称就能通过审核的保证。失败任务是否计费未核验，不声称免费。真实任务标识、私有路径和运行记录不纳入分发包。

## 本轮离线验证

44 项测试覆盖均通过：先完成 43 项全量测试，再加入原创来源交接包测试并通过全部 13 项 runner 测试；其他未改动模块复用前次结果。覆盖 HTTP 客户端合约、错误脱敏、预算与断点续跑、真实 FFmpeg 媒体处理、本地中文语音、内容交接包和来源类型校验。外部视频响应为模拟，本轮未增加任何付费生成或平台上传。

本轮将网络改编、原创与用户供稿分开校验：网络改编必须有真实 URL，后两类不再被迫伪造链接。交接包发布文案兼容非网络来源。另修正本地系统配音对以连字符开头的外部台词的处理，避免将其误作读取本地文件的参数；该入口有回归测试，真实中文配音测试亦通过。

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
python3 scripts/package_skill.py --output dist/jokes.zip
```

Skill 格式、相对文件引用和分享包另行检查。安装包由显式清单构建，排除环境密钥、私有运行区、聊天记录和浏览器登录态；这是当前交付文件检查，不是对所有历史提交或使用者未来素材的无泄漏保证。

## 流程展示

本轮使用 Archify workflow v2 生成 HTML，确定性检查、真实浏览器证据和视觉观察分别记录在 [流程展示验收](../docs/workflow-verification.md)。README 嵌入静态预览并链接可交互 HTML；来源和许可证见 [第三方致谢](../THIRD_PARTY_NOTICES.md)。

前一轮只交付本地 Skill。v1.3.0 发布阶段补充新手教程、供应商通用说明和公开案例，四份案例视频均通过解码检查；代码没有新增改动，复用上述测试结果。发布范围包含 GitHub 源码、案例与 Release 安装包，不创建每日定时任务或社交平台内容；远端结果以 Release 和对应提交为准。
